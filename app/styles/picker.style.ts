import { StyleSheet } from 'react-native';

export const pickerSelectStyles = StyleSheet.create({
    inputIOS: {
        fontSize: 14,
        padding: 10,
        borderWidth: 1,
        borderColor: 'gray',
        borderRadius: 2,
        color: 'black',
        marginHorizontal: 10,
    },
    inputAndroid: {
        fontSize: 14,
        padding: 10,
        borderWidth: 1,
        borderColor: 'gray',
        borderRadius: 2,
        color: 'black',
        marginHorizontal: 10,
    },
});
